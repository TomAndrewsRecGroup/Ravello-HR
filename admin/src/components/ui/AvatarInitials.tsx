// Re-export the shared component so existing `@/components/ui/AvatarInitials`
// imports continue to work without churning every callsite.
export { default } from '@shared/components/ui/AvatarInitials';
